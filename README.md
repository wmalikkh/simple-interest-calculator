# Simple Interest Calculator

A Bash script that calculates simple interest based on user-provided principal amount, rate of interest, and time period.

## Description
This project is a command-line Simple Interest Calculator built with a Bash script (`simple-interest.sh`). It prompts the user for three inputs — principal, rate of interest, and time period — and calculates the simple interest using the formula:
